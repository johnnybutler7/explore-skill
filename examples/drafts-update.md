# Drafts Update

```bash
explore drafts update --account acme-careers --draft 12 --input tmp/draft-update.json --json
```

Use this when an open draft should stay private but needs a better title, summary, or attribute payload before any future review.
