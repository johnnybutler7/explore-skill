# Create Draft

```bash
explore content create-draft --account acme-careers --input tmp/draft.json --json
```

Use this when the user wants a new future content item prepared without publishing it live.
